## Configuración de Mako basada en estilo Nord/Waybar
# --- Fuente y Texto ---
# La misma fuente y tamaño que tu Waybar
font=JetBrainsMono Nerd Font 13
# Color del texto (blanco)
text-color=#ffffff
# Alineación del texto
text-alignment=left

# --- Fondo y Bordes ---
# Color de fondo: #2b303b (tu rgba 43,48,59)
# La terminación '80' es la transparencia (aprox 50% como pediste)
# Si lo quieres más opaco, cambia '80' por 'CC' o 'FF'
background-color=#2b303b80

# Aunque tu Waybar no tiene bordes, en las notificaciones flotantes
# suele quedar bien un borde sutil para separarlo del fondo.
# He usado el color #64727D (tu botón 'focused')
border-size=2
border-color=#64727d
border-radius=5

# --- Dimensiones y Espaciado ---
width=350
height=150
margin=10
padding=15
# Espacio para el icono
icons=1
max-icon-size=48

# --- Comportamiento ---
# Tiempo por defecto (5 segundos)
default-timeout=5000
# Agrupar notificaciones
group-by=app-name

# --- Estilos por Urgencia ---

# Urgencia Baja (ej. Spotify, info del sistema)
# Usamos el azul nórdico (#81a1c1) de tu calendario para el borde
[urgency=low]
border-color=#81a1c1

# Urgencia Normal (Mensajes, correos)
[urgency=normal]
border-color=#64727d

# Urgencia Crítica (Errores, batería baja)
# Usamos el rojo (#f53c3c) de tu red desconectada
[urgency=critical]
border-color=#f53c3c
text-color=#f53c3c
default-timeout=0
